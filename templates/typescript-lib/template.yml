# https://moonrepo.dev/docs/config/template
$schema: '../../.moon/cache/schemas/template.json'

description: 'Scaffolds the initial structure for a TypeScript library'
destination: 'libs/typescript/[name]'
title: 'TypeScript Library'
variables:
  name:
    default: ''
    prompt: 'Library name ? (without typescript-)'
    required: true
    type: 'string'
