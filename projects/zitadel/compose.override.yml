# Development environment override (local only — Dokploy deploys compose.yml).
services:
  zitadel:
    ports:
      - '8080:8080/tcp'
    networks:
      - lychen-network

  db:
    ports:
      - '24432:5432/tcp'
    networks:
      - lychen-network

  provisioner:
    networks:
      - lychen-network

networks:
  lychen-network:
    external: true
